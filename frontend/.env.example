# Frontend environment variables
VITE_API_URL=http://localhost:5001/api

# Where detection snapshots are served from, if they are exposed to the browser.
# Blank means the alerts console shows "snapshot recorded but not reachable"
# rather than a broken image.
VITE_SNAPSHOT_BASE_URL=
